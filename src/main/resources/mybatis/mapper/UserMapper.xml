<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.testboard.mapper.UserMapper">
    <select id="login" parameterType="UserDTO" resultType="UserDTO">
        SELECT id, email, username
        FROM tusers
        WHERE username = #{username}
        AND password_hash = #{password}
    </select>

    <insert id="register" parameterType="UserDTO">
        INSERT INTO tusers (email, username, password_hash, created_date)
        VALUES (#{email}, #{username}, #{password}, CURRENT_TIMESTAMP)
    </insert>

    <select id="existsByUsername" parameterType="String" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM tusers WHERE username = #{username}
        )
    </select>

    <select id="existsByEmail" parameterType="String" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM tusers WHERE email = #{email}
        )
    </select>

    <select id="findByUsername" parameterType="String" resultType="UserDTO">
        SELECT id, email, username, password_hash as password
        FROM tusers
        WHERE username = #{username}
    </select>
</mapper>
